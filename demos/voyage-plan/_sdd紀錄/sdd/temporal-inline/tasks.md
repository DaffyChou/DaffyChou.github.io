# tasks：temporal-inline（Demo merged／final 版）

- [x] 1. `dimRowHTML` 新增「有效期間」行：長期（常設）／限期摘要（至 迄 · 事由 · 到期後回 …）＋「＋ 限期調整」按鈕與子面板容器；CSS（含手機版）
- [x] 2. 子面板內容：該維度限期項目列表（生效中／未開始；已到期收合）、新增表單（W、C、起、迄、事由；無維度下拉）；[移除] 依 `canClearVessel`
- [x] 3. `renderPerVesselSetup` 改為逐列傳入時效項目、移除 `temporalBlockHTML` 呼叫；`wirePerVesselSetup` 改掛子面板事件（展開／收合狀態記 session、新增、移除）；驗證沿用（迄>起、事由必填、C>W）
- [x] 4. 待確認 2 決議：同一維度×航段同時只允許一筆生效／排定，新增期間重疊即拒絕（未回覆，採建議值）
- [x] 5. 文件同步：規範頁 §6、操作流程頁組員第 7 步、截圖版第 7 步重截圖、Q&A 頁 Q4 文字；merged 與 final 同步
- [x] 6. UX 複審（桌面／手機、三角色、深淺色、console）＋ 回歸：時效 > 常設、到期自動失效、代理人可新增、主管不可移除

## 驗收條件
- 單船設定頁不再有獨立的「時效性覆寫」區塊。
- 每個維度列有「有效期間：長期（常設）」；點「＋ 限期調整」在該列內展開，新增一筆後：列標頭徽章變「時效·○○」、有效期間行顯示「限期 至 … · 事由 · 到期後回 …」。
- 沒有限期項目的維度列外觀與現在一致（多一行文字，不多輸入框）。
- 組員／代理人可新增、可移除；主管可新增、看不到移除。
- 到期後徽章回「單船」或「全船隊／系統預設」，列表中該筆進「已到期」收合。
- 覆寫總覽仍列出時效項目；變更紀錄 layer=temporal 正常。
- 桌面與手機、深淺色皆無 console 錯誤與版面破版。
